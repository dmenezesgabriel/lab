[project]
name = "lab-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "pytest-bdd",
    "docker",
    "playwright",
    "pytest-playwright",
    "httpx",
]

[tool.uv]
package = false

[tool.uv.pip]
require-hashes = true
